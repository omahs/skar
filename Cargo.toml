[workspace]
resolver = "2"

members = [
    "skar",
    "format",
    "rpc-client",
    "rpc-types",
    "ingest",
    "net-types",
]

[profile.maxperf]
inherits = "release"
lto = true
codegen-units = 1
